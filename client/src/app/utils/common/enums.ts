export enum ViewType {
    Grid = 'grid',
    List = 'list'
}

export enum ActionType {
    Create = 'create',
    Edit = 'edit',
    Delete = 'delete'
}

export enum Page {
    List = 'list',
    Details = 'details'
}

export enum SortDirection {
    ASC = 'asc',
    DESC = 'desc'
}